export interface Virksomhed {
  cvr: number;
  navn: string;
  adresse: string;
  postnummer: number;
  by: string;
}

export interface VirksomhedInTypeInput {
  cvr: number;
  navn: string;
  adresse: string;
  postnummer: number;
  by: string;
}
